export const JOB_STATUS = {
  Active: "Active",
  Inactive: "Inactive",
  Draft: "Draft",
} as const;
export const JOB_STATUS_ENUM = [
  JOB_STATUS.Active,
  JOB_STATUS.Inactive,
  JOB_STATUS.Draft,
] as const;
